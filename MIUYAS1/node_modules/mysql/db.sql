--创建数据库
create database stusys character set utf8;

--切换到数据库statys
use statys

--创建班级信息表
create table classInfo(
    cid int primary key auto_increment,--主键列  自动增长
    cname varchar(100) not null unique,--唯一  不能为空
    status int  --班级状态
);

--修改自增的起始值
alter table classInfo auto_increment=1001;

--创建学生信息表
create table stuInfo(
    sid int primary key auto_increment,
    sname varchar(100) not null,
    cid int,
    sex varchar(4),
    age int,
    pwd varchar(100),
    tel varchar(15)
)

--修改学号的开始值
alter table stuInfo auto_increment=10001;

--添加班级的初始数据   注意：mysql中自增列用0代替
insert into classInfo values(0,'YC24',1);
insert into classInfo values(0,'YC23',1);
insert into classInfo values(0,'YC25',1);

--添加学生的初始数据
insert into stuInfo values(0,'飞飞',1001,'男',20,'aaaa','11111111111');